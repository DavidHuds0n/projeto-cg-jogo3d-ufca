/**
 * @file Interactable.cpp
 * @brief Arquivo de implementação para objetos interativos.
 *
 * @note Este arquivo está atualmente vazio porque a definição em "Interactable.h"
 * contém apenas uma `struct` (Interactable) e um `enum` (InteractableType).
 * Como `structs` em C++ por padrão têm todos os seus membros públicos e não
 * possuem lógica complexa (métodos), geralmente não é necessário um arquivo .cpp.
 * Se no futuro os interativos ganharem funções mais complexas, a implementação
 * delas viria aqui.
 */
